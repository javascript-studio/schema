{
  "name": "@studio/schema",
  "version": "1.2.0",
  "description": "Plain JavaScript objects that throw if invalid or unknown properties are read, assigned or deleted",
  "main": "lib/schema.js",
  "scripts": {
    "lint": "eslint .",
    "test": "mocha && mochify",
    "watch": "mocha --watch",
    "posttest": "npm run lint",
    "preversion": "npm test",
    "version": "changes --commits --footer",
    "postversion": "git push --follow-tags && npm publish"
  },
  "keywords": [
    "schema",
    "json",
    "object",
    "validate",
    "validation",
    "proxy"
  ],
  "author": "Maximilian Antoni <max@javascript.studio>",
  "homepage": "https://github.com/javascript-studio/studio-schema",
  "eslintConfig": {
    "extends": "@studio"
  },
  "devDependencies": {
    "@sinonjs/referee-sinon": "^9.0.2",
    "@studio/changes": "^2.2.0",
    "@studio/eslint-config": "^1.0.2",
    "eslint": "^7.26.0",
    "mocha": "^5.2.0",
    "mochify": "^7.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/javascript-studio/studio-schema.git"
  },
  "files": [
    "lib",
    "LICENSE",
    "README.md"
  ],
  "license": "MIT"
}
